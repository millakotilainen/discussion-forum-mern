{
  "name": "forum",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "(cd apps/api && npm run dev) & (cd apps/web && npm run dev)",
    "lint": "(cd apps/api && npm run lint) && (cd apps/web && npm run lint)",
    "format": "prettier . --write",
    "typecheck": "(cd apps/api && npm run typecheck) && (cd apps/web && npm run typecheck)",
    "test": "(cd apps/api && npm run test) && (cd apps/web && npm run test)"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.2",
    "postcss": "^8.5.8",
    "prettier": "^3.8.1",
    "tailwindcss": "^4.2.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
